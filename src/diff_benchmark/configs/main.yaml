defaults:
  - dataset: hcp
  - model: 2dcnn
  - pred_head: binary_classification
  - backend: torch
  - data: default
  - target: gender
  - cluster: margaret
  - _self_

random_state: 42
runtime:
  force: false
  run_id: null
  experiment_hash: null
  # fold_idx: null


choices:
  model: [pca_forest, forest] #[medicalnet] #[pca_linear] #[2dcnn, medicalnet]
  backend: [sklearn]
  dataset: [hcp] #[camcan] #, hcp, wand] #
  # target: [age]
  # pred_head: [regression]
  # data.batch_size: [1]
  cluster: [margaret_cpu]
  # cluster.conf.parallel_type: [None]
  dataset.metric_to_compute: [md]
  dataset.tissue_type: [white]
  # pred_head: [regression]
  # target: [age]
  # cluster: [margaret]
  # model.backbone.depth: [10, 18]
  ########
  # model: [linear, pca_linear, forest, pca_forest] #[2dcnn, medicalnet]
  # backend: [sklearn]
  # dataset: [wand]
  # cluster: [margaret_cpu]
  # dataset.metric_to_compute: [md]
  ######
  # model: [dinov2]
  # # cluster.conf.parallel_type: [None]
  # cluster.slurm_cfg.timeout_min: [4920]
  # dataset.metric_to_compute: [md]
  # # model.backbone.depth: [10]
  # data.batch_size: [4]
  # dataset: [hcp]
  # data.num_workers: [13]
  