defaults:
  - dataset: hcp
  - model: 2dcnn
  - pred_head: binary_classification
  - backend: torch
  - data: default
  - target: gender
  # - training: default
  # - experiment: 2dcnn_slices
  - cluster: margaret
  - _self_

random_state: 42
runtime:
  force: false
  run_id: null
  experiment_hash: null
  # fold_idx: null


choices:
  model: [medicalnet] #[2dcnn, medicalnet]
  cluster: [jean_zay]
  dataset.metric_to_compute: [rtop]
  model.backbone.depth: [10, 18]
  