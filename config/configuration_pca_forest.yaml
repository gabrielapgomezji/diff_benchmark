# ==============================
# MODEL CONFIGURATION
# ==============================
pretrain_path: /data/parietal/store3/work/ggomezji/projects/diff_benchmark/MedicalNet_pytorch_files2/pretrain/resnet_10.pth

models:
- name: pca_forest
  params:
    name: pca_forest
    comment: PCA random forest reg. Test new conf
    backend: 
      backend: sklearn
      num_classes: 1
    data:
      batch_size: 32
    backbone:
      prediction_task: regression