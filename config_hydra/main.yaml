defaults:
  - dataset: hcp
  - model: 2dcnn
  - pred_head: binary_classification
  - backend: torch
  - data: default
  - target: gender
  # - training: default
  # - experiment: 2dcnn_slices
  - cluster: margaret
  - _self_

runtime:
  run_id: null
  # fold_idx: null