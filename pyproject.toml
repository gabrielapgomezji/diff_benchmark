[project]
name = "diff-benchmark"
version = "0.1.0"
description = ""
authors = [
    {name = "ggomezji",email = "gabriela.gomez-jimenez@inria.fr"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "diff_benchmark", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
