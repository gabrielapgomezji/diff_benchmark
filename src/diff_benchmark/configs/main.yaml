defaults:
  - dataset: hcp
  - model: 2dcnn
  - pred_head: binary_classification
  - backend: torch
  - data: default
  - target: gender
  # - training: default
  # - experiment: 2dcnn_slices
  - cluster: margaret
  - _self_

random_state: 42
runtime:
  force: false
  run_id: null
  experiment_hash: null
  # fold_idx: null


choices:
  model: [dummy_classifier] #[2dcnn, medicalnet]
  backend: [sklearn]
  dataset: [camcan]
  cluster: [margaret]
  cluster.conf.parallel_type: [None]
  dataset.metric_to_compute: [md]
  # # model.backbone.depth: [10, 18]
  # model: [dummy_classifier, linear, pca_linear, forest, pca_forest] #[2dcnn, medicalnet]
  # backend: [sklearn]
  # cluster: [margaret_cpu]
  # dataset.metric_to_compute: [md]
  