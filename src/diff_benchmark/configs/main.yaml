defaults:
  - dataset: hcp
  - model: 2dcnn
  - pred_head: binary_classification
  - backend: torch
  - data: default
  - target: gender
  # - training: default
  # - experiment: 2dcnn_slices
  - cluster: margaret
  - _self_

random_state: 42
runtime:
  run_id: null
  # fold_idx: null

# pred_head:
#   dropout: [0, 0.5]

# backend:
#   lr: [1e-4, 1e-5]